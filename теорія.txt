Описати своїми словами навіщо потрібні функції у програмуванні.
- функції потрібні, наприклад, для виконання однакових дій, але з різними значеннями, в різних частинах коду. Функція - наче шаблон,
який можна використати в будь-якій частині коду 
Описати своїми словами, навіщо у функцію передавати аргумент.
- Аргумент це очікуване значення , яке передається при виклику функції, щоб вона працювала
Що таке оператор return та як він працює всередині функції?
-return повертає значення , цим завершується "чиста" функція